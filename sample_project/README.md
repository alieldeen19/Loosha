# Minimal Security Scan Sample

This sample project is intentionally tiny and includes:

- a harmless Python test file
- an intentionally reviewable pattern (`shell=True`) so repository security/code scanning can report a finding safely

No real secrets, credentials, or sensitive data are included.
